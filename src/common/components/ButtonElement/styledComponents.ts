import styled from "styled-components";
import tw from "twin.macro";

export const StyledButtonElement = styled.button`

${tw `text-white text-sm px-5 bg-blue py-3 font-semibold border-0 rounded mx-3 cursor-pointer`}`