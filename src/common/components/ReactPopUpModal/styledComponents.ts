import styled from "styled-components";
import tw from "twin.macro";

export const StyledCloseButtonContainer = styled.div`${tw`flex items-center`}
justify-content: flex-end`

export const StyledCloseButton = styled.button`${tw`bg-transparent border-0 cursor-pointer`}
`

export const StyeledReactModalContainer= styled.div`${tw ``}`