import styled from "styled-components";
import tw from "twin.macro";

export const StyledTextArea = styled.textarea`${tw`border-0 w-full font-sans flex items-center p-2 py-4 text-base`}
outline: none`